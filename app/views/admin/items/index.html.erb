<div class="container">
  <div class="row">
    <h4 class="mb-5 mt-5"><span class="bg-secondary">商品一覧</span></h4>
  </div>
  <div class="row">
    <% @items.each do |item| %>
    <%= item.name %>
    <%= item.introduction %>
    <%= item.genre_id %>
    <%= item.price %>
    <%= item.is_active %>
    <%#= link_to "編集する", edit_admin_genre_path(genre),class:"btn btn-success" %>
    <% end %>
  </div>
</div>
